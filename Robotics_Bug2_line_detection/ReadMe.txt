/***********************Read Me File***********************************/
1. Source Files : bug2.cpp , linedetect.cpp  (header - gprot.h)
2. Launch Files - 
	a) bug2.launch (roslaunch lab2 bug2.launch)
		It will open up stage map and bug2 algorithm.
	b) linedetect.launch (roslaunch lab2 linedetect.launch)
		It will open up stage map, rviz and linedetect algorithm. 
	c) Extra - 
		test.launch (roslaunch lab2 test.launch)
		It will open up stage map, rviz, bug2 and linedetect algorithm all simultaneously.
